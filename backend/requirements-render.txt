# Ultra-Lightweight requirements for Render deployment
# CRITICAL: Pre-compiled wheels ONLY for Python 3.11+
# Pure Python packages only - NO Rust/C/Fortran compilation!

# FastAPI and Web Framework
fastapi==0.100.0
uvicorn==0.23.0

# Authentication
python-jose==3.3.0
passlib==1.7.4

# Database (SQLite only - no asyncpg which requires C)
sqlalchemy==2.0.0

# Utilities (pure Python versions)
pydantic==1.10.12
python-dotenv==1.0.0
loguru==0.7.2
